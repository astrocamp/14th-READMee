<div class="area2">
  <% if @resume.area_2 == 1 %>

  <div class="flex flex-col mt-8">
    <div class="pl-2 font-bold">工作經驗</div>
    <div>
      <div class="flex items-center">

        <div class="">
          <% if @resume.work_exp_start_date_1.present? %>
            <%= f.text_field :work_exp_start_date_1,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
          <% else %>
            <% if @resume.profile.work_experiences.first.present? %>
              <%= f.text_field :work_exp_start_date_1, value: @resume.profile.work_experiences.first.start_date,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
            <% else %>
              <%= f.text_field :work_exp_start_date_1,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
            <% end %>
          <% end %>
        </div>
        <span>~</span>
        <div>
          <% if @resume.work_exp_end_date_1.present? %>
          <%= f.text_field :work_exp_end_date_1,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束日期" %>
          <% else %>
            <% if @resume.profile.work_experiences.first.present? %>
              <%= f.text_field :work_exp_end_date_1, value: @resume.profile.work_experiences.first.end_date,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束日期" %>
            <% else %>
              <%= f.text_field :work_exp_start_date_1,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束時間" %>
            <% end %>
          <% end %>
        </div>

      </div>

      <% if @resume.work_exp_name_1.present? %>
        <%= f.text_field :work_exp_name_1,  class: "w-full p-2 mt-1 font-semibold rounded-md resize-y  border-white ", placeholder: "工作名稱" %>
      <% else %>
        <% if @resume.profile.work_experiences.first.present? %>
          <%= f.text_field :work_exp_name_1, value: @resume.profile.work_experiences.first.title,  class: "w-full p-2 mt-1 font-semibold rounded-md resize-y  border-white ", placeholder: "工作名稱" %>
        <% else %>
          <%= f.text_field :work_exp_name_1,  class: "w-full p-2 font-semibold mt-1 rounded-md resize-y  border-white ", placeholder: "工作名稱" %>
        <% end %>
      <% end %>

    </div>
    <%= f.text_area :work_exp_content_1, class: "w-full h-18 my-1 p-2 rounded-md resize-y  border-white", placeholder: "工作內容，内容不能超過120個字" %>

    <div>
      <div class="flex items-center" >

        <% if @resume.work_exp_start_date_2.present? %>
          <%= f.text_field :work_exp_start_date_2,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
        <% else %>
          <% if @resume.profile.work_experiences.second.present? %>
            <%= f.text_field :work_exp_start_date_2, value: @resume.profile.work_experiences.second.start_date,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
          <% else %>
            <%= f.text_field :work_exp_start_date_2,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "開始日期" %>
          <% end %>
        <% end %>
        <span>~</span>
        <% if @resume.work_exp_end_date_2.present? %>
          <%= f.text_field :work_exp_end_date_2,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束日期" %>
        <% else %>
          <% if @resume.profile.work_experiences.second.present? %>
            <%= f.text_field :work_exp_end_date_2, value: @resume.profile.work_experiences.second.end_date,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束日期" %>
          <% else %>
            <%= f.text_field :work_exp_end_date_2,  class: "px-2 input_w rounded-md resize-y  border-white", placeholder: "結束日期" %>
          <% end %>
        <% end %>
      </div>

      <% if @resume.work_exp_name_2.present? %>
        <%= f.text_field :work_exp_name_2,  class: "w-full grow font-semibold p-2 rounded-md resize-y my-1 border-white ", placeholder: "工作名稱" %>
      <% else %>
        <% if @resume.profile.work_experiences.second.present? %>
          <%= f.text_field :work_exp_name_2, value: @resume.profile.work_experiences.second.title,  class: "w-full grow my-1 font-semibold p-2 rounded-md resize-y  border-white ", placeholder: "工作名稱" %>
        <% else %>
          <%= f.text_field :work_exp_name_2,  class: "w-full grow font-semibold p-2 rounded-md resize-y my-1  border-white ", placeholder: "工作名稱" %>
        <% end %>
      <% end %>
    </div>
    <%= f.text_area :work_exp_content_2,  class: "grow h-18 mb-1 p-2 rounded-md resize-y  border-white ", placeholder: "工作內容，内容不能超過120個字" %>
  </div>

  <% else @resume.area_2 == 3 %>

    <div>
      <div class="flex flex-col mt-8">
        <h1 class="pl-2 font-bold">工作經驗</h1>
        <div class="justify-between md:flex">

          <% if @resume.work_exp_name_1.present? %>
            <%= f.text_field :work_exp_name_1,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗一" %>
          <% else %>
            <% if @resume.profile.work_experiences.first.present? %>
              <%= f.text_field :work_exp_name_1, value: @resume.profile.work_experiences.first.title,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗一" %>
            <% else %>
              <%= f.text_field :work_exp_name_1,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗一" %>
            <% end %>
          <% end %>
          <div class="flex items-center" >

            <% if @resume.work_exp_start_date_1.present? %>
              <%= f.text_field :work_exp_start_date_1,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始日期" %>
            <% else %>
              <% if @resume.profile.work_experiences.first.present? %>
                <%= f.text_field :work_exp_start_date_1, value: @resume.profile.work_experiences.first.start_date,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始日期" %>
              <% else %>
                <%= f.text_field :work_exp_start_date_1,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始日期" %>
              <% end %>
            <% end %>
            <span>~</span>
            <% if @resume.work_exp_end_date_1.present? %>
              <%= f.text_field :work_exp_end_date_1,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束日期" %>
            <% else %>
              <% if @resume.profile.work_experiences.first.present? %>
                <%= f.text_field :work_exp_end_date_1, value: @resume.profile.work_experiences.first.end_date,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束日期" %>
              <% else %>
                <%= f.text_field :work_exp_start_date_1,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束時間" %>
              <% end %>
            <% end %>

          </div>
        </div>
        <%= f.text_area :work_exp_content_1,  class: "my-2 p-2 h-18 rounded-md resize-y border-0 border-white", placeholder: "工作內容，内容不能超過120個字" %>
      </div>
      <div class="flex flex-col">
        <div class="justify-between md:flex">

          <% if @resume.work_exp_name_2.present? %>
            <%= f.text_field :work_exp_name_2,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗二" %>
          <% else %>
            <% if @resume.profile.work_experiences.second.present? %>
              <%= f.text_field :work_exp_name_2, value: @resume.profile.work_experiences.second.title,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗二" %>
            <% else %>
              <%= f.text_field :work_exp_name_2,  class: "grow mb-1 p-2 rounded-md resize-y  border-white", placeholder: "工作經驗二" %>
            <% end %>
          <% end %>
          <div class="flex items-center" >

            <% if @resume.work_exp_start_date_2.present? %>
              <%= f.text_field :work_exp_start_date_2,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始時間" %>
            <% else %>
              <% if @resume.profile.work_experiences.second.present? %>
                <%= f.text_field :work_exp_start_date_2, value: @resume.profile.work_experiences.second.start_date,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始時間" %>
              <% else %>
                <%= f.text_field :work_exp_start_date_2,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "開始時間" %>
              <% end %>
            <% end %>
            <span>~</span>
            <% if @resume.work_exp_end_date_2.present? %>
              <%= f.text_field :work_exp_end_date_2,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束日期" %>
            <% else %>
              <% if @resume.profile.work_experiences.second.present? %>
                <%= f.text_field :work_exp_end_date_2, value: @resume.profile.work_experiences.second.end_date,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束日期" %>
              <% else %>
                <%= f.text_field :work_exp_end_date_2,  class: "w-28 h-6 rounded-md resize-y border-white", placeholder: "結束日期" %>
              <% end %>
            <% end %>

          </div>
        </div>
        <%= f.text_area :work_exp_content_2, class: "w-full h-18 my-2 p-2 rounded-md resize-y  border-white", placeholder: "工作內容，内容不能超過120個字" %>
      </div>
    </div>
  <% end %>
</div>