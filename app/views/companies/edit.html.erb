<div class="container mx-auto w-2/3 p-3 text-left">
  <%= form_with(model: @company, url: company_path, data:{ turbo:false, method:'patch' }) do |f| %>
    <h1 class="text-center text-5xl">編輯公司</h1>
    <%= render 'form', f: f %>
  <% end %>
</div>
