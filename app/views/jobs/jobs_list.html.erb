<div>
  <div class="max-w-[85rem] mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-10">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-gray-800 sm:text-6xl dark:text-white">
        職缺列表
      </h1>
      <p class="mt-3 text-gray-600 dark:text-white">
        為自己找一份好工作
      </p>

      <div class="relative max-w-xl mx-auto mt-7 sm:mt-12">
        <!-- Form -->
        <form>
          <div class="relative z-10 flex space-x-3 p-3 bg-white border rounded-lg shadow-lg shadow-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:shadow-gray-900/[.2]">
            <%= search_form_for @choose, url: jobs_list_path do |f| %>
              <div class="flex-[1_0_0%]">
                <%= f.search_field :title_or_content_or_company_name_cont, class: "block w-full p-3 border-transparent rounded-md focus:border-blue-500 focus:ring-blue-500 dark:bg-gray-800 dark:text-gray-400", placeholder: "職缺名稱、內容、公司名稱" %>
              </div>
              <div class="flex-[0_0_auto]">
                <%= f.submit class:"inline-flex items-center justify-center gap-2 p-4 text-sm font-semibold text-white transition-all bg-amber-500 border border-transparent rounded-md hover:bg-amber-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 dark:focus:ring-offset-gray-800 cursor-pointer" %>
              </div>
            <% end %>
          </div>
        </form>
      </div>
    </div>
  </div>

  <div>
    <% @jobs_list.each do |job| %>
      <ul class="justify-between p-5 m-2 leading-6 ">
        <div>
          <li class="border shadow-lg md:flex">
            <div class="p-6">
              <img src="https://picsum.photos/100/100/?random=10">
            </div>
            <div class="p-2 md:w-2/3 ">
              <%= link_to job.title, job_show_path(id: job.id), class: "text-2xl text-amber-500" %>
              <div class="mt-2 ">
                <%= link_to "#{job.company.name}", company_show_path(job.company.id) %>
              </div>
              <div class="mt-2">
                <%= link_to job_show_path(job.id) do %>
                  <%= truncate(job.content, length: 200) %>
                <% end %>
              </div>
            </div>
            <div class="p-2 md:w-1/3">
              <div>需求人數: <%= job.num_of_people %></div>
              <div>年資: <%= job.seniority %></div>
              <div>薪水: <%= job.salary %></div>
              <div>地址: <%= job.company.address %></div>
              <%= form_with(model: job, data: { turbo: false}, url: job_seeker_list_path, method: :post) do |f| %>
                <%= hidden_field_tag :job, job.id %>
                <%= hidden_field_tag :company, job.company_id %>
                <% if current_user.role == "job_seeker" %>
                <%= f.submit "馬上應徵", class: "top-4 right-80 inline-block px-5 py-3 mx-auto text-center text-white rounded-lg cursor-pointer bg-amber-500 hover:bg-amber-800" %>   
                <% end %>
              <% end %>              
            </div>
          </li>
        </div>
      </ul>
    <% end %>
  </div>
</div>