<div>
  <h1 class="font-bold text-4xl">Pages#home</h1>
  <div>
    <% if current_user %>
      <p>Hello <%= current_user.email %>!</p>

    <% else %>
      <p>you are not signed in.</p>
    <% end %>
  </div>

  <div class= 'flex justify-end'>
    <div>
      <p class = 'text-2xl font-bold'>註冊</p>
    </div>

    <div> 
      <%= render 'devise/registrations/new', resource: User.new, resource_name: User.new %>
    </div>

  </div>
</div>
